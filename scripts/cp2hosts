#!/bin/bash

maq_file=~/rodar_tracy/maquinas
maquinas=$(cat  $maq_file | grep "lnls\|sgi" | grep -v "#")
a=($( hostname ));

for maq in $maquinas; do
	echo $maq
	if [ "$maq" != "$a" ];then
		scp $1 $maq:$2;
	fi
    
done
